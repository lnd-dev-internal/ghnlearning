import type { Metadata } from "next";
export const metadata: Metadata = { title: "Nhân viên Xử lý B2B" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
